<%= render 'users/dashboard_nav_buttons', user: @user, selected: :followers %>
    
    <div class="row">
        <div class="col-sm-3"></div>
        <div class="col-sm-6">
                <% @followers.each do |follower| %>
                    <div class="bottom-space-md">
                        <%= render 'users/summary', user: follower %>
                    </div>
                <% end %>
        </div>
        <div class="col-sm-3"></div>
    
    </div>
